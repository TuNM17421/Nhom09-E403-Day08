# Day 08 Lab - RAG Pipeline
# Cai dat: pip install -r requirements.txt

# === Core ===
python-dotenv>=1.0.0

# === LLM Providers (chon it nhat mot) ===
openai>=1.0.0                    # Option A: OpenAI API
google-generativeai>=0.5.0       # Option B: Google Gemini

# === Vector Store ===
chromadb>=0.5.0                  # Vector database (local, khong can server)

# === Embeddings - chon mot trong hai ===
# Option A: OpenAI Embeddings (dung chung API key voi LLM)
# (da co trong openai package)

# Option B: Sentence Transformers (local, khong can API key)
sentence-transformers>=2.7.0     # Model: paraphrase-multilingual-MiniLM-L12-v2

# === Sprint 3: Sparse/Hybrid Retrieval ===
rank-bm25>=0.2.2                 # BM25 keyword search

# === Sprint 3: Rerank ===
# sentence-transformers da bao gom CrossEncoder

# === Web UI ===
fastapi>=0.100.0                 # API backend
uvicorn>=0.20.0                  # ASGI server
sse-starlette>=1.0.0             # Server-Sent Events cho streaming

# === Utils ===
tqdm>=4.66.0                     # Progress bar khi index nhieu docs
